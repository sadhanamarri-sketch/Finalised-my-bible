<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- BibleDataImportWorker (see data/BibleDataImportWorker.kt) runs the
         one-time KJV/Greek/Hebrew/cross-reference download as WorkManager
         foreground work so it survives the app being backgrounded — both
         permissions are required for a foreground service of type
         "dataSync" on Android 14+ (API 34). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Legacy .widget.BibleWidgetProvider (plain RemoteViews) has been
             deleted outright, per user request, along with its layout/
             drawable resources and app-side wiring. VerseOfDayWidgetReceiver
             (Glance) was later deleted too, once ContinueReadingWidget's own
             4x2 expanded state (pill + Highlighted/Studied/Notes/Search
             cards) fully covered what it offered — this is now the app's
             only home screen widget. -->
        <receiver
            android:name=".widget.ContinueReadingWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/continue_reading_widget_info" />
        </receiver>

        <receiver
            android:name=".reminders.ReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".reminders.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- WorkManager's own manifest declares this service untyped;
             BibleDataImportWorker's setForeground() call passes
             FOREGROUND_SERVICE_TYPE_DATA_SYNC at runtime, but Android 14+
             (API 34) also requires the type to be declared here — this
             overrides (merges into) WorkManager's entry rather than
             replacing it. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>

</manifest>
